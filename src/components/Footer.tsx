/**
 * Footer component — re-exports FooterSection which contains the full
 * footer implementation (app name, description, nav links, copyright).
 * All copy is sourced from i18n (landing.footer namespace).
 *
 * Requirements: 8.1, 8.2, 8.3
 */
export { default } from "@/app/sections/FooterSection";
